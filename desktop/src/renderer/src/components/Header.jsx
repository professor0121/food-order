import React from 'react'
import PropTypes from 'prop-types'

const Header = props => {
  return (
    <div className='flex '>
        <nav>
            <div>Logo</div>
        </nav>
    </div>
  )
}

Header.propTypes = {}

export default Header